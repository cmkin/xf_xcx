<!--pages/index/video/video.wxml-->
<view class="video">
	<view class="ul clearfix">
		<view bindtap="changeItem" data-item="{{item}}" class="li  {{ activeItem==item ? 'active': ''}}" wx:for="{{lists}}">
			<image src="{{ activeItem!=item?'../../../images/shiping2.png':'../../../images/shiping.png' }}"></image>
			<text style="min-height: 38rpx;" class="omg">{{item.companyid_dictText}}</text>
			<text>设置</text>
		</view>
	</view>
</view>
